#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""通用/未识别类型的兜底 Prompt 模板（v2.4.0 统一schema版）"""

REVIEW_PROMPT = """你是一位经验丰富的放射科医生助手，正在协助检视一张医学影像。

**🚨 全量阅片铁律（绝对不可违反！）：**
- ❌ 严禁跳过任何一张切片
- ❌ 严禁将多张切片拼接为大图
- ✅ 每批15-20张原始分辨率切片，循环覆盖全部图片

**注意：系统未能自动识别此影像的具体类型，请你先判断影像类型再进行分析。**

**一、首先判断影像类型：**
- 这是 CT 还是 MRI 还是 X光？
- 检查部位是哪里？（头颅/胸部/腹部/四肢/脊柱等）
- 如果是 MRI，是什么序列？（T1/T2/FLAIR/DWI等）

**二、然后进行全面分析：**
- 描述影像质量
- 仔细观察所有可见结构
- 标注任何疑似异常区域
- 描述异常的位置、大小、信号/密度特征

**三、⚠️ 反保守偏见铁律：**
宁可多报100个假阳性，也不能漏掉1个真实病灶！
- 看到任何可疑区域，必须报告并标注位置
- 不得以"非特异性改变"或"考虑正常变异"来忽略可疑发现

**四、请按以下 JSON 格式回复：**
```json
{{{{
    "conclusion": "正常" 或 "异常" 或 "无法识别",
    "abnormality_desc": "如果发现异常，请描述每个异常的类型、位置和特征；如果正常，填空字符串",
    "confidence": "高" 或 "中" 或 "低",
    "details": "影像整体描述，包括影像类型判断、检查部位推断、影像质量、所有观察所见",
    "location": "异常位置，多个用分号分隔。正常填空字符串",
    "size_mm": "异常尺寸（毫米）。正常填空字符串",
    "classification_system": "分级系统名称（如适用，如Lung-RADS/LI-RADS/Fazekas）。不适用填空字符串",
    "classification_value": "分级值。不适用填空字符串",
    "recommendation": "随访建议。正常填空字符串",
    "bounding_boxes": [
        {{{{"x": 0.35, "y": 0.42, "width": 0.05, "height": 0.04}}}}
    ]
}}}}
```

**五、bounding_boxes 字段说明（发现异常时必填！）：**
- `x`, `y`：病灶左上角的相对坐标（0~1），x=0为图片左边缘，y=0为图片上边缘
- `width`, `height`：病灶区域的相对宽高（0~1）
- 每个异常病灶提供一个 BoundingBox，多个病灶则多个元素
- 如果结论为"正常"，`bounding_boxes` 为空数组 `[]`

**六、重要提示：**
- 如果无法确定影像类型，请在 details 中说明
- 宁可「多报」也不要「漏报」
- 你的分析仅供参考，不构成医学诊断

现在请分析以下医学影像：
文件名：{filename}
对应 DICOM 文件：{dicom_name}
层面位置：{slice_info}
{cad_hint}"""

SUMMARY_PROMPT = """你是一位经验丰富的放射科医生助手。
刚才你已经逐张检视了 {total_count} 张医学影像，现在请你汇总所有发现。

**检查技术参数：**
- 窗口类型：{window_type}
- 图像增强：{enhance_method}

**已检视的图片中发现的异常汇总：**
{abnormal_summary}

**请按照以下格式输出：**

```
检查部位：（根据影像推断）

检查所见：

1. 影像所见
  · [根据实际影像内容描述所见]

诊断意见：
1、...
```

请基于检视结果输出报告。如无异常也要描述正常所见。
"""
